flask>=2.3.0
flask_sqlalchemy>=3.1.0
flask_cors>=4.0.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
python-dateutil>=2.8.2
sqlalchemy>=2.0.23
gunicorn>=21.2.0
pandas>=2.0.0
openpyxl>=3.1.0
pillow>=10.0.0
requests>=2.31.0
# ── CoG solver acceleration ───────────────────────────────────────
# KD-tree O(N log N) neighbour index + compiler-allocated thread pool
scipy>=1.11.0
# JIT compilation of the inner best-neighbour ascent loop (~2x speedup
# for large parcel sets; falls back gracefully if unavailable)
numba>=0.59.0
