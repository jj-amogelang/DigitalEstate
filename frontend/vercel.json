{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/((?!static|favicon|manifest|robots|logo|asset-manifest).*)", "destination": "/index.html" }
  ]
}